# OS / Editor
.DS_Store
Thumbs.db
desktop.ini
.vscode/*
!.vscode/settings.json
*.swp
*.swo

# Environment / secrets
.env
.env.*
!.env.example
*.pem
*.key
*.crt

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# PHP / Composer cache or vendor (if you switch to Composer install)
vendor/
composer.phar

# Node artifacts (if frontend tooling is added later)
node_modules/
dist/
build/

# Runtime / cache / temp
tmp/
temp/
cache/
*.cache

# Test / coverage
coverage/
.phpunit.result.cache

# XAMPP / local uploads (if created)
uploads/
